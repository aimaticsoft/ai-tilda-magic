

## Валидация телефона + ускорение отправки

### 1. Валидация номера телефона (клиент)

В `src/components/ContactsSection.tsx`:
- Добавить маску ввода и валидацию перед отправкой
- Минимум 10 цифр (российский формат: +7XXXXXXXXXX или 8XXXXXXXXXX)
- Показывать ошибку под полем если номер невалидный
- Разрешить только цифры, +, пробелы, скобки, дефисы
- Regex: после удаления всех нецифровых символов должно остаться минимум 10 цифр

```typescript
const isValidPhone = (phone: string) => {
  const digits = phone.replace(/\D/g, '');
  return digits.length >= 10 && digits.length <= 15;
};
```

- Добавить состояние `phoneError` и показывать красное сообщение под полем
- Блокировать кнопку отправки пока телефон невалидный

### 2. Валидация на сервере

В `supabase/functions/send-telegram-notification/index.ts`:
- Добавить проверку длины номера (минимум 10 цифр) на стороне сервера тоже

### 3. Ускорение отправки

Сейчас задержка может быть из-за холодного старта edge-функции. Два улучшения:
- **Оптимистичный UI**: показать `showSuccess` сразу после вызова `invoke()`, не дожидаясь ответа. Ошибки обработать отдельно.
- Т.е. сначала показываем анимацию успеха, а fetch идёт в фоне. Если ошибка — показываем toast.

### Файлы для изменения
1. `src/components/ContactsSection.tsx` — валидация + оптимистичный UI
2. `supabase/functions/send-telegram-notification/index.ts` — серверная валидация телефона

